# 后端反馈密钥，用于提交反馈时生成 HMAC-SHA256 签名
# 获取方式：联系管理员或从后端配置中获取
VITE_FEEDBACK_SECRET=

# 本地开发时使用的设备 ID，用于获取统计数据
# 生产环境会自动从登录用户信息中获取，此变量仅用于本地开发测试
# 获取方式：登录后在浏览器控制台执行 localStorage.getItem('zeka_device_id')
VITE_DEVICE_ID=
